- name: demo on shell commands
  hosts: local
  connection: local
  tasks: 
  - name: command test
    #command: cat /etc/password | grep
    shell: cat /etc/passwd | grep root
    register: user_output

  - name: print output 
    debug: 
      msg: "{{user_output}}"